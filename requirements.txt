fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0
pydantic-settings>=2.5,<3.0
psycopg[binary]>=3.2,<4.0

# InsightFace provides detection, alignment, and 512-D embeddings.
insightface>=1.0.1,<2.0
onnxruntime>=1.19,<2.0
# InsightFace 1.x depends on the regular OpenCV wheel. Declaring the headless
# wheel here too would install two packages that provide the same cv2 module.
opencv-python>=5.0,<6.0
numpy>=1.26,<3.0
pillow>=10.4,<13.0
psutil>=6.0,<8.0
